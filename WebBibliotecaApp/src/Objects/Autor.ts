export class Autor {
    codAutor = 0;
    nome: string = '';
    descricao: string = '';

    constructor (codAutor: number, nome: string, descricao: string){
        this.codAutor = codAutor;
        this.descricao
        this.nome
    }
}
